<%= form_for @update, :html => { :class => 'form-horizontal form-model' } do |f| %>
  <div class="form-group">
    <%= f.label :progress, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_area :progress, :class => 'text-field form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :pains, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_area :pains, :class => 'text-field form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :plans, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_area :plans, :class => 'text-field form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class='col-md-offset-2 col-md-10'>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  updates_path, :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
